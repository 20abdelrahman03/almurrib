# Tiny RPG Maker Fixture (MV layout)

Deterministic `www/data/*.json` sample exercising the RPG Maker adapter:
event dialogue (101/401), choices (102), JS plugin lines (355, skipped),
actor names/nicknames/profiles, item names/descriptions, system title and
terms, map names and display names, common events.
