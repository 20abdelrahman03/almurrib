# Non-dialogue content: nothing here should be extracted as translatable.

define config.name = _("Tiny Fixture")
define config.version = "1.0"

image bg street = "bg/street.png"

init python:
    config.screen_width = 1280
